/*
 * File: currentUser.js
 * Project: react-blog
 * Created: Thursday, July 28th 2022, 2:21:22 pm
 * Last Modified: Thursday, July 28th 2022, 3:01:03 pm
 * Copyright © 2022 AMDE Agência
 */

import React, {createContext, useState} from 'react'

// Array with value and setter createContext([{}, () => {}])
export const CurrentUserContext = createContext([{}, () => {}])

// This is a wrapper for all components to sharing data
export const CurrentUserProvider = ({children}) => {
  const [state, setState] = useState({
    isLoading: false,
    isLoggedIn: null,
    currentUser: null,
  })

  return (
    <CurrentUserContext.Provider value={[state, setState]}>
      {children}
    </CurrentUserContext.Provider>
  )
}
