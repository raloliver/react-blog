/*
 * File: errorMessage.js
 * Project: react-blog
 * Created: Thursday, July 28th 2022, 3:49:01 pm
 * Last Modified: Thursday, July 28th 2022, 4:02:37 pm
 * Copyright © 2022 AMDE Agência
 */

import React from 'react'

const ErrorMessages = ({messages}) => {
  const errorMessages = Object.keys(messages).map((key) => {
    const message = messages[key].join(' ')

    return `${key}: ${message}`
  })
  return (
    <ul className="error-messages">
      {errorMessages.map((message) => {
        return <li key={message}>{message}</li>
      })}
    </ul>
  )
}

export default ErrorMessages
